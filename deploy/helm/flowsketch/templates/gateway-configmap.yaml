{{- if .Values.gateway.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flowsketch.fullname" . }}-gateway
  labels:
    {{- include "flowsketch.labels" . | nindent 4 }}
    app.kubernetes.io/component: gateway
data:
  gateway.yaml: |
    gateway:
      listen: 0.0.0.0:{{ int64 .Values.gateway.listenPort }}
      seed: {{ int64 .Values.gateway.seed }}
      staleAfterMs: {{ int64 .Values.gateway.staleAfterMs }}
      maxNodes: {{ int64 .Values.gateway.maxNodes }}
      maxRetainedSketchBytes: {{ int64 .Values.gateway.maxRetainedSketchBytes }}
    queries:
      {{- range $name, $_ := .Values.queries }}
      - file: /etc/flowsketch/queries/{{ $name }}
      {{- end }}
{{- end }}
